(self["webpackChunkreact_uni_comps"] = self["webpackChunkreact_uni_comps"] || []).push([["mf-dep_vendors-node_modules_dumi-theme-mobile_es_layouts_demo_js"],{

/***/ "./node_modules/dumi-theme-mobile/es/layouts/demo.js":
/*!***********************************************************!*\
  !*** ./node_modules/dumi-theme-mobile/es/layouts/demo.js ***!
  \***********************************************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "ROUTE_MSG_TYPE": function() { return /* binding */ ROUTE_MSG_TYPE; }
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var dumi__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! dumi */ "./src/.umi/core/umiExports.ts");
/* harmony import */ var dumi_theme__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! dumi/theme */ "./node_modules/dumi/node_modules/@umijs/preset-dumi/lib/theme/index.js");
/* harmony import */ var f2_touchemulator__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! f2-touchemulator */ "./node_modules/f2-touchemulator/dist/index.umd.js");
/* harmony import */ var f2_touchemulator__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(f2_touchemulator__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var umi_hd__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! umi-hd */ "./node_modules/umi-hd/lib/vl.js");
/* harmony import */ var umi_hd__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(umi_hd__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var umi_hd_lib_flex__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! umi-hd/lib/flex */ "./node_modules/umi-hd/lib/flex.js");
/* harmony import */ var umi_hd_lib_flex__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(umi_hd_lib_flex__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var umi_hd_lib_vw__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! umi-hd/lib/vw */ "./node_modules/umi-hd/lib/vw.js");
/* harmony import */ var umi_hd_lib_vw__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(umi_hd_lib_vw__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var umi_hd_lib_vh__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! umi-hd/lib/vh */ "./node_modules/umi-hd/lib/vh.js");
/* harmony import */ var umi_hd_lib_vh__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(umi_hd_lib_vh__WEBPACK_IMPORTED_MODULE_6__);
function _toConsumableArray(arr) {
  return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread();
}

function _nonIterableSpread() {
  throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}

function _unsupportedIterableToArray(o, minLen) {
  if (!o) return;
  if (typeof o === "string") return _arrayLikeToArray(o, minLen);
  var n = Object.prototype.toString.call(o).slice(8, -1);
  if (n === "Object" && o.constructor) n = o.constructor.name;
  if (n === "Map" || n === "Set") return Array.from(o);
  if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen);
}

function _iterableToArray(iter) {
  if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter);
}

function _arrayWithoutHoles(arr) {
  if (Array.isArray(arr)) return _arrayLikeToArray(arr);
}

function _arrayLikeToArray(arr, len) {
  if (len == null || len > arr.length) len = arr.length;

  for (var i = 0, arr2 = new Array(len); i < len; i++) {
    arr2[i] = arr[i];
  }

  return arr2;
}









var ROUTE_MSG_TYPE = 'dumi:update-iframe-route'; // available HD modes

var HD_MODES = {
  vl: (umi_hd__WEBPACK_IMPORTED_MODULE_3___default()),
  flex: (umi_hd_lib_flex__WEBPACK_IMPORTED_MODULE_4___default()),
  vw: (umi_hd_lib_vw__WEBPACK_IMPORTED_MODULE_5___default()),
  vh: (umi_hd_lib_vh__WEBPACK_IMPORTED_MODULE_6___default())
};

var MobileDemoLayout = function MobileDemoLayout(_ref) {
  var children = _ref.children;

  var _useContext = (0,react__WEBPACK_IMPORTED_MODULE_0__.useContext)(dumi_theme__WEBPACK_IMPORTED_MODULE_1__.context),
      config = _useContext.config;

  var target = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var _ref2 = config.theme,
      _ref2$hd = _ref2.hd;
  _ref2$hd = _ref2$hd === void 0 ? {} : _ref2$hd;
  var _ref2$hd$rules = _ref2$hd.rules,
      rules = _ref2$hd$rules === void 0 ? [{
    mode: 'vw',
    options: [100, 750]
  }] : _ref2$hd$rules;
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    // Simulate the touch event of mobile terminal
    if (target.current) {
      // fix https://github.com/umijs/dumi/issues/996
      f2_touchemulator__WEBPACK_IMPORTED_MODULE_2___default()(document);
    } // listen route change message


    var handler = function handler(ev) {
      if (ev.data.type === ROUTE_MSG_TYPE) {
        dumi__WEBPACK_IMPORTED_MODULE_7__.history.push(ev.data.value);
      }
    };

    window.addEventListener('message', handler);
    return function () {
      return window.removeEventListener('message', handler);
    };
  }, []);
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    var handler = function handler() {
      var clientWidth = document.documentElement.clientWidth;
      rules // discard invalid rules
      .filter(function (rule) {
        return HD_MODES[rule.mode];
      }) // match first valid rule
      .some(function (rule) {
        if ( // without min & max width
        Number.isNaN(rule.minWidth * 1) && Number.isNaN(rule.maxWidth * 1) || // min width only
        Number.isNaN(rule.maxWidth * 1) && clientWidth > rule.minWidth || // max width only
        Number.isNaN(rule.minWidth * 1) && clientWidth < rule.maxWidth || // both min & max width
        clientWidth > rule.minWidth && clientWidth < rule.maxWidth) {
          HD_MODES[rule.mode].apply(HD_MODES, _toConsumableArray([].concat(rule.options)));
          document.documentElement.setAttribute('data-scale', 'true');
          return true;
        }
      });
    };

    handler();
    window.addEventListener('resize', handler);
    return function () {
      return window.removeEventListener('resize', handler);
    };
  }, [rules]);
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "__dumi-default-mobile-demo-layout",
    ref: target
  }, children);
};

/* harmony default export */ __webpack_exports__["default"] = (MobileDemoLayout);

/***/ }),

/***/ "./node_modules/f2-touchemulator/dist/index.umd.js":
/*!*********************************************************!*\
  !*** ./node_modules/f2-touchemulator/dist/index.umd.js ***!
  \*********************************************************/
/***/ (function(module) {

(function (global, factory) {
   true ? module.exports = factory() : 0;
})(this, function () {
  'use strict';
  /**
   * Emulate touch on desktop
   */

  var eventTarget;
  /**
   * create an touch point
   * @constructor
   * @param target
   * @param identifier
   * @param pos
   * @param deltaX
   * @param deltaY
   * @returns {Object} touchPoint
   */

  var Touch = function Touch(target, identifier, pos, deltaX, deltaY) {
    deltaX = deltaX || 0;
    deltaY = deltaY || 0;
    this.identifier = identifier;
    this.target = target;
    this.clientX = pos.clientX + deltaX;
    this.clientY = pos.clientY + deltaY;
    this.screenX = pos.screenX + deltaX;
    this.screenY = pos.screenY + deltaY;
    this.pageX = pos.pageX + deltaX;
    this.pageY = pos.pageY + deltaY;
  };
  /**
   * create empty touchlist with the methods
   * @class
   * @return { Array } touchList
   */


  function TouchList() {
    var touchList = [];

    touchList.item = function item(index) {
      return this[index] || null;
    }; // specified by Mozilla


    touchList.identifiedTouch = function identifiedTouch(id) {
      return this[id + 1] || null;
    };

    return touchList;
  }
  /**
   * Simple trick to fake touch event support
   * this is enough for most libraries like Modernizr and Hammer
   */


  function fakeTouchSupport() {
    var objs = [window, document.documentElement];
    var props = ['ontouchstart', 'ontouchmove', 'ontouchcancel', 'ontouchend'];

    for (var o = 0; o < objs.length; o++) {
      for (var p = 0; p < props.length; p++) {
        if (objs[o] && objs[o][props[p]] === undefined) {
          objs[o][props[p]] = null;
        }
      }
    }
  }
  /**
   * only trigger touches when the left mousebutton has been pressed
   * @param { String } touchType touchType
   * @return { Function } handler
   */


  function onMouse(touchType) {
    return function (ev) {
      // prevent mouse events
      if (ev.which !== 1) {
        return;
      } // The EventTarget on which the touch point started when it was first placed on the surface,
      // even if the touch point has since moved outside the interactive area of that element.
      // also, when the target doesnt exist anymore, we update it


      if (ev.type === 'mousedown' || !eventTarget || eventTarget && !eventTarget.dispatchEvent) {
        eventTarget = ev.target;
      }

      triggerTouch(touchType, ev); // reset

      if (ev.type === 'mouseup') {
        eventTarget = null;
      }
    };
  }
  /**
   * trigger a touch event
   * @param { String } eventName eventName
   * @param { Object } mouseEv mouseEv
   */


  function triggerTouch(eventName, mouseEv) {
    var touchEvent = document.createEvent('Event');
    touchEvent.initEvent(eventName, true, true);
    touchEvent.altKey = mouseEv.altKey;
    touchEvent.ctrlKey = mouseEv.ctrlKey;
    touchEvent.metaKey = mouseEv.metaKey;
    touchEvent.shiftKey = mouseEv.shiftKey;
    touchEvent.touches = getActiveTouches(mouseEv);
    touchEvent.targetTouches = getActiveTouches(mouseEv);
    touchEvent.changedTouches = createTouchList(mouseEv);
    eventTarget.dispatchEvent(touchEvent);
  }
  /**
   * create a touchList based on the mouse event
   * @param { Object } mouseEv mouseEv
   * @return { TouchList } touchList
   */


  function createTouchList(mouseEv) {
    var touchList = TouchList();
    touchList.push(new Touch(eventTarget, 1, mouseEv, 0, 0));
    return touchList;
  }
  /**
   * receive all active touches
   * @param { Object } mouseEv mouseEv
   * @return { TouchList } touchList
   */


  function getActiveTouches(mouseEv) {
    // empty list
    if (mouseEv.type === 'mouseup') {
      return TouchList();
    }

    return createTouchList(mouseEv);
  }
  /**
   * desktop initializer
   * @param { HTMLElement } target target
   */


  function TouchEmulator(target) {
    fakeTouchSupport();
    target.addEventListener('mousedown', onMouse('touchstart'), true);
    target.addEventListener('mousemove', onMouse('touchmove'), true);
    target.addEventListener('mouseup', onMouse('touchend'), true);
  } // start distance when entering the multitouch mode


  TouchEmulator.multiTouchOffset = 75;
  return TouchEmulator;
});

/***/ }),

/***/ "./node_modules/umi-hd/lib/flex.js":
/*!*****************************************!*\
  !*** ./node_modules/umi-hd/lib/flex.js ***!
  \*****************************************/
/***/ (function(module, exports) {

"use strict";

/**
 * @param {Number} [baseFontSize = 100] - 基础fontSize, 默认100px;
 * @param {Number} [fontscale = 1] - 有的业务希望能放大一定比例的字体;
 */

Object.defineProperty(exports, "__esModule", ({
  value: true
}));
var win = window;

exports.default = win.flex = function (baseFontSize, fontscale) {
  var _baseFontSize = baseFontSize || 100;

  var _fontscale = fontscale || 1;

  var doc = win.document;
  var ua = navigator.userAgent;
  var matches = ua.match(/Android[\S\s]+AppleWebkit\/(\d{3})/i);
  var UCversion = ua.match(/U3\/((\d+|\.){5,})/i);
  var isUCHd = UCversion && parseInt(UCversion[1].split('.').join(''), 10) >= 80;
  var isIos = navigator.appVersion.match(/(iphone|ipad|ipod)/gi);
  var dpr = win.devicePixelRatio || 1;

  if (!isIos && !(matches && matches[1] > 534) && !isUCHd) {
    // 如果非iOS, 非Android4.3以上, 非UC内核, 就不执行高清, dpr设为1;
    dpr = 1;
  }

  var scale = 1 / dpr;
  var metaEl = doc.querySelector('meta[name="viewport"]');

  if (!metaEl) {
    metaEl = doc.createElement('meta');
    metaEl.setAttribute('name', 'viewport');
    doc.head.appendChild(metaEl);
  }

  metaEl.setAttribute('content', 'width=device-width,user-scalable=no,initial-scale=' + scale + ',maximum-scale=' + scale + ',minimum-scale=' + scale + ',viewport-fit=cover');
  doc.documentElement.style.fontSize = _baseFontSize / 2 * dpr * _fontscale + 'px';
};

module.exports = exports['default'];

/***/ }),

/***/ "./node_modules/umi-hd/lib/vh.js":
/*!***************************************!*\
  !*** ./node_modules/umi-hd/lib/vh.js ***!
  \***************************************/
/***/ (function(module, exports) {

"use strict";

/**
 * @param {Number} [baseFontSize = 100] - 基础fontSize, 默认50px, 对于iPhone的设计稿, 1rem=100px; 方便裸算rem; 有的是16px, 用浏览器默认;
 * @param {Number} [psdHeight = 1270] - 设计稿默认宽度, 以750*(1334-64)(64为iPhone头顶间距)为基准;
 */

Object.defineProperty(exports, "__esModule", ({
  value: true
}));
var win = window;

exports.default = win.vh = function (baseFontSize, psdHeight) {
  var _baseFontSize = baseFontSize || 100; // 如果是iPhone5的稿子, 这里写1072!


  var _psdHeight = psdHeight || 1270;

  var doc = win.document;
  var ua = navigator.userAgent;
  var matches = ua.match(/Android[\S\s]+AppleWebkit\/(\d{3})/i);
  var UCversion = ua.match(/U3\/((\d+|\.){5,})/i);
  var isUCHd = UCversion && parseInt(UCversion[1].split('.').join(''), 10) >= 80;
  var isIos = navigator.appVersion.match(/(iphone|ipad|ipod)/gi);
  var dpr = win.devicePixelRatio || 1;
  var docEl = doc.documentElement; // 为了消除安卓dpr乱标的比例

  var rate = 1;
  var scale = 1 / dpr;

  if (isIos) {// iOS下不用做什么
  } else if (matches && matches[1] > 534 || isUCHd) {
    // 有些兼容环境下, fontSize为100px的时候, 结果1rem=86px; 需要纠正viewport;
    docEl.style.fontSize = _baseFontSize + 'px';
    var div = doc.createElement('div');
    div.setAttribute('style', 'width: 1rem;display:none');
    docEl.appendChild(div);
    var trueWidth = win.getComputedStyle(div).width;
    docEl.removeChild(div); // 如果1rem的真实px跟html.fontSize不符. 那么就要加一个rate缩放了;

    if (trueWidth !== docEl.style.fontSize) {
      var trueWidthVal = parseInt(trueWidth, 10);
      rate = _baseFontSize / trueWidthVal;
      scale = scale * rate;
    }
  } else {
    // 如果是在PC或者安卓4.3(会闪屏)以下, 则正常展现.
    scale = 1;
  }

  var metaEl = doc.querySelector('meta[name="viewport"]');

  if (!metaEl) {
    metaEl = doc.createElement('meta');
    metaEl.setAttribute('name', 'viewport');
    doc.head.appendChild(metaEl);
  }

  metaEl.setAttribute('content', 'width=device-width,user-scalable=no,initial-scale=' + scale + ',maximum-scale=' + scale + ',minimum-scale=' + scale + ',viewport-fit=cover'); // width/750*100, 为了统一rem为0.01rem = 1px

  var setFontSize = function setFontSize() {
    docEl.style.fontSize = _baseFontSize / _psdHeight * docEl.clientHeight * rate + 'px';
  };

  setFontSize();
  win.addEventListener('resize', setFontSize);
};

module.exports = exports['default'];

/***/ }),

/***/ "./node_modules/umi-hd/lib/vl.js":
/*!***************************************!*\
  !*** ./node_modules/umi-hd/lib/vl.js ***!
  \***************************************/
/***/ (function(module, exports) {

"use strict";

/**
 * @param {Number} [baseFontSize = 100] - 基础fontSize, 默认50px, 对于iPhone的设计稿, 1rem=100px; 方便裸算rem; 有的是16px, 用浏览器默认;
 * @param {Number} [psdWidth = 750] - 设计稿默认宽度, 以750为基准;
 */

Object.defineProperty(exports, "__esModule", ({
  value: true
}));
var win = window;

exports.default = win.vl = function (baseFontSize, psdWidth) {
  var _baseFontSize = baseFontSize || 100;

  var _psdWidth = psdWidth || 750;

  var doc = win.document;
  var ua = navigator.userAgent;
  var matches = ua.match(/Android[\S\s]+AppleWebkit\/(\d{3})/i);
  var UCversion = ua.match(/U3\/((\d+|\.){5,})/i);
  var isUCHd = UCversion && parseInt(UCversion[1].split('.').join(''), 10) >= 80;
  var docEl = doc.documentElement;
  var rate = 1;

  if (matches && matches[1] > 534 || isUCHd) {
    // 有些兼容环境下, fontSize为100px的时候, 结果1rem=86px; 需要纠正viewport;
    docEl.style.fontSize = _baseFontSize + 'px';
    var div = doc.createElement('div');
    div.setAttribute('style', 'width: 1rem;display:none');
    docEl.appendChild(div);
    var trueWidth = win.getComputedStyle(div).width;
    docEl.removeChild(div); // 如果1rem的真实px跟html.fontSize不符. 那么就要加一个rate缩放了;

    if (trueWidth !== docEl.style.fontSize) {
      var trueWidthVal = parseInt(trueWidth, 10);
      rate = _baseFontSize / trueWidthVal;
    }
  }

  var metaEl = doc.querySelector('meta[name="viewport"]');

  if (!metaEl) {
    metaEl = doc.createElement('meta');
    metaEl.setAttribute('name', 'viewport');
    doc.head.appendChild(metaEl);
  }

  metaEl.setAttribute('content', 'width=device-width,user-scalable=no,initial-scale=1,maximum-scale=1,minimum-scale=1'); // width/750*100, 为了统一rem为0.01rem = 1px

  var setFontSize = function setFontSize() {
    docEl.style.fontSize = _baseFontSize / _psdWidth * docEl.clientWidth * rate + 'px';
  };

  setFontSize();
  win.addEventListener('resize', setFontSize);
};

module.exports = exports['default'];

/***/ }),

/***/ "./node_modules/umi-hd/lib/vw.js":
/*!***************************************!*\
  !*** ./node_modules/umi-hd/lib/vw.js ***!
  \***************************************/
/***/ (function(module, exports) {

"use strict";

/**
 * @param {Number} [baseFontSize = 100] - 基础fontSize, 默认50px, 对于iPhone的设计稿, 1rem=100px; 方便裸算rem; 有的是16px, 用浏览器默认;
 * @param {Number} [psdWidth = 750] - 设计稿默认宽度, 以750为基准;
 */

Object.defineProperty(exports, "__esModule", ({
  value: true
}));
var win = window;

exports.default = win.vw = function (baseFontSize, psdWidth) {
  var _baseFontSize = baseFontSize || 100;

  var _psdWidth = psdWidth || 750;

  var doc = win.document;
  var ua = navigator.userAgent;
  var matches = ua.match(/Android[\S\s]+AppleWebkit\/(\d{3})/i);
  var UCversion = ua.match(/U3\/((\d+|\.){5,})/i);
  var isUCHd = UCversion && parseInt(UCversion[1].split('.').join(''), 10) >= 80;
  var isIos = navigator.appVersion.match(/(iphone|ipad|ipod)/gi);
  var dpr = win.devicePixelRatio || 1;
  var docEl = doc.documentElement; // 为了消除安卓dpr乱标的比例

  var rate = 1;
  var scale = 1 / dpr;

  if (isIos) {// iOS下不用做什么
  } else if (matches && matches[1] > 534 || isUCHd) {
    // 有些兼容环境下, fontSize为100px的时候, 结果1rem=86px; 需要纠正viewport;
    docEl.style.fontSize = _baseFontSize + 'px';
    var div = doc.createElement('div');
    div.setAttribute('style', 'width: 1rem;display:none');
    docEl.appendChild(div);
    var trueWidth = win.getComputedStyle(div).width;
    docEl.removeChild(div); // 如果1rem的真实px跟html.fontSize不符. 那么就要加一个rate缩放了;

    if (trueWidth !== docEl.style.fontSize) {
      var trueWidthVal = parseInt(trueWidth, 10);
      rate = _baseFontSize / trueWidthVal;
      scale = scale * rate;
    }
  } else {
    // 如果是在PC或者安卓4.3(会闪屏)以下, 则正常展现.
    scale = 1;
  }

  var metaEl = doc.querySelector('meta[name="viewport"]');

  if (!metaEl) {
    metaEl = doc.createElement('meta');
    metaEl.setAttribute('name', 'viewport');
    doc.head.appendChild(metaEl);
  }

  metaEl.setAttribute('content', 'width=device-width,user-scalable=no,initial-scale=' + scale + ',maximum-scale=' + scale + ',minimum-scale=' + scale + ',viewport-fit=cover'); // width/750*100, 为了统一rem为0.01rem = 1px

  var setFontSize = function setFontSize() {
    docEl.style.fontSize = _baseFontSize / _psdWidth * docEl.clientWidth * rate + 'px';
  };

  setFontSize();
  win.addEventListener('resize', setFontSize);
};

module.exports = exports['default'];

/***/ })

}]);